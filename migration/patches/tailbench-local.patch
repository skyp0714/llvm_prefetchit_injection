diff --git a/tailbench/Makefile.config b/tailbench/Makefile.config
index a9f2c85..47b11c5 100644
--- a/tailbench/Makefile.config
+++ b/tailbench/Makefile.config
@@ -1,3 +1,7 @@
 # Set this to point to the top level installation directory of the Java
-# Development Kit. Only needed for Specjbb
+# Development Kit. Only needed for Specjbb.
+ifneq ("$(wildcard ../../.local/jdk8/bin/java)","")
+JDK_PATH=../../.local/jdk8
+else
 JDK_PATH=/usr/lib/jvm/java-8-openjdk-amd64
+endif
diff --git a/tailbench/configs.sh b/tailbench/configs.sh
index cdc936f..80112d2 100644
--- a/tailbench/configs.sh
+++ b/tailbench/configs.sh
@@ -1,9 +1,20 @@
-# Set this to point to the top level of the TailBench data directory
-DATA_ROOT=/home/tailbench/Tailbench/tailbench.inputs
+# TailBench top-level paths.
+TAILBENCH_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
+TAILBENCH_HOME="$( cd "${TAILBENCH_ROOT}/.." && pwd )"
+
+# Set this to point to the top level of the TailBench data directory.
+DATA_ROOT=${TAILBENCH_HOME}/tailbench.inputs
 
 # Set this to point to the top level installation directory of the Java
-# Development Kit. Only needed for Specjbb
-JDK_PATH=/usr/lib/jvm/java-8-openjdk-amd64
+# Development Kit. Only needed for Specjbb.
+if [[ -d ${TAILBENCH_HOME}/.local/jdk8 ]]; then
+    JDK_PATH=${TAILBENCH_HOME}/.local/jdk8
+elif [[ -d /usr/lib/jvm/java-8-openjdk-amd64 ]]; then
+    JDK_PATH=/usr/lib/jvm/java-8-openjdk-amd64
+else
+    JDK_PATH=/usr/lib/jvm/java-11-openjdk-amd64
+fi
 
 # This location is used by applications to store scratch data during execution.
-SCRATCH_DIR=/home/tailbench/Tailbench/scratch
+SCRATCH_DIR=${TAILBENCH_HOME}/scratch
+mkdir -p "${SCRATCH_DIR}"
diff --git a/tailbench/img-dnn/run.sh b/tailbench/img-dnn/run.sh
index 60b0518..1653e4f 100755
--- a/tailbench/img-dnn/run.sh
+++ b/tailbench/img-dnn/run.sh
@@ -3,10 +3,13 @@
 DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
-THREADS=32
-REQS=100000000 # Set this very high; the harness controls maxreqs
+THREADS=${THREADS:-32}
+REQS=${REQS:-100000000} # Set this very high; the harness controls maxreqs
+QPS=${QPS:-500}
+WARMUPREQS=${WARMUPREQS:-5000}
+MAXREQS=${MAXREQS:-12500}
 
-TBENCH_WARMUPREQS=5000 TBENCH_MAXREQS=12500 TBENCH_QPS=500 \
+TBENCH_WARMUPREQS=${WARMUPREQS} TBENCH_MAXREQS=${MAXREQS} TBENCH_QPS=${QPS} \
     TBENCH_MINSLEEPNS=10000 TBENCH_MNIST_DIR=${DATA_ROOT}/img-dnn/mnist \
     ./img-dnn_integrated -r ${THREADS} \
     -f ${DATA_ROOT}/img-dnn/models/model.xml -n ${REQS}
diff --git a/tailbench/masstree/run.sh b/tailbench/masstree/run.sh
index 2b21c48..51cb162 100755
--- a/tailbench/masstree/run.sh
+++ b/tailbench/masstree/run.sh
@@ -2,9 +2,9 @@
 
 if [[ -z "${NTHREADS}" ]]; then NTHREADS=32; fi
 
-QPS=2000
-MAXREQS=50000
-WARMUPREQS=14000
+QPS=${QPS:-2000}
+MAXREQS=${MAXREQS:-50000}
+WARMUPREQS=${WARMUPREQS:-14000}
 
 TBENCH_QPS=${QPS} TBENCH_MAXREQS=${MAXREQS} TBENCH_WARMUPREQS=${WARMUPREQS} \
     TBENCH_MINSLEEPNS=10000 ./mttest_integrated -j${NTHREADS} \
diff --git a/tailbench/moses/run.sh b/tailbench/moses/run.sh
index e3f4c08..da4f188 100755
--- a/tailbench/moses/run.sh
+++ b/tailbench/moses/run.sh
@@ -3,10 +3,10 @@
 DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
-THREADS=32
-QPS=100
-WARMUPREQS=5000
-MAXREQS=5000
+THREADS=${THREADS:-32}
+QPS=${QPS:-100}
+WARMUPREQS=${WARMUPREQS:-5000}
+MAXREQS=${MAXREQS:-5000}
 
 BIN=./bin/moses_integrated
 
diff --git a/tailbench/shore/run.sh b/tailbench/shore/run.sh
index 0e4a195..2e0fce1 100755
--- a/tailbench/shore/run.sh
+++ b/tailbench/shore/run.sh
@@ -3,12 +3,12 @@
 DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
-THREADS=32
-QPS=10
-WARMUPREQS=250
-MAXREQS=500
+THREADS=${THREADS:-32}
+QPS=${QPS:-10}
+WARMUPREQS=${WARMUPREQS:-250}
+MAXREQS=${MAXREQS:-500}
 
-DUMMYREQS=1000000 # set this really high so MAXREQS controls execution
+DUMMYREQS=${DUMMYREQS:-1000000} # set this really high so MAXREQS controls execution
 
 # Point this to an appropriate location on your system
 # note: instead of setting it here, we better get it from ../configs.sh
diff --git a/tailbench/silo/Makefile b/tailbench/silo/Makefile
index d5339b5..11547b6 100644
--- a/tailbench/silo/Makefile
+++ b/tailbench/silo/Makefile
@@ -76,6 +76,7 @@ else
 endif
 
 CXXFLAGS := -g -Wall -std=c++0x
+CXXFLAGS += $(EXTRA_CXXFLAGS)
 CXXFLAGS += -MD -Ithird-party/lz4 -DCONFIG_H=\"$(CONFIG_H)\"
 ifeq ($(DEBUG_S),1)
         CXXFLAGS += -fno-omit-frame-pointer -DDEBUG
diff --git a/tailbench/silo/benchmarks/bench.cc b/tailbench/silo/benchmarks/bench.cc
index f9be747..85aec67 100644
--- a/tailbench/silo/benchmarks/bench.cc
+++ b/tailbench/silo/benchmarks/bench.cc
@@ -29,6 +29,10 @@ extern "C" int mallctl(const char *name, void *oldp, size_t *oldlenp, void *newp
 #include "request.h"
 #include "tbench_server.h"
 
+#ifndef SILO_TXN_FN_PREFETCH_DISTANCE
+#define SILO_TXN_FN_PREFETCH_DISTANCE 0
+#endif
+
 using namespace std;
 using namespace util;
 
@@ -131,6 +135,13 @@ bench_worker::run()
     tBenchRecvReq(reinterpret_cast<void**>(&req));
     Response resp;
 retry:
+#if SILO_TXN_FN_PREFETCH_DISTANCE
+    const size_t prefetch_type = req->type;
+    if (prefetch_type < workload.size()) {
+      bench_worker::txn_fn_t prefetch_fn = workload[prefetch_type].fn;
+      __builtin_prefetch(reinterpret_cast<const void *>(prefetch_fn), 0, 3);
+    }
+#endif
     timer t;
     const unsigned long old_seed = r.get_seed();
     const auto ret = workload[req->type].fn(this);
diff --git a/tailbench/silo/run.sh b/tailbench/silo/run.sh
index 6ab3e35..7452615 100755
--- a/tailbench/silo/run.sh
+++ b/tailbench/silo/run.sh
@@ -1,12 +1,12 @@
 #!/bin/bash
 # ops-per-worker is set to a very large value, so that TBENCH_MAXREQS controls how
 # many ops are performed
-NUM_WAREHOUSES=4
-NUM_THREADS=32
+NUM_WAREHOUSES=${NUM_WAREHOUSES:-4}
+NUM_THREADS=${NUM_THREADS:-32}
 
-QPS=1000
-MAXREQS=50000
-WARMUPREQS=20000
+QPS=${QPS:-1000}
+MAXREQS=${MAXREQS:-50000}
+WARMUPREQS=${WARMUPREQS:-20000}
 
 TBENCH_QPS=${QPS} TBENCH_MAXREQS=${MAXREQS} TBENCH_WARMUPREQS=${WARMUPREQS} \
     TBENCH_MINSLEEPNS=10000 \
diff --git a/tailbench/silo/util.h b/tailbench/silo/util.h
index a63f7ae..15e525f 100644
--- a/tailbench/silo/util.h
+++ b/tailbench/silo/util.h
@@ -305,6 +305,14 @@ private:
   unsigned long seed;
 };
 
+template <typename A, typename B>
+inline std::ostream &
+operator<<(std::ostream &o, const std::pair<A, B> &p)
+{
+  o << "[" << p.first << ", " << p.second << "]";
+  return o;
+}
+
 template <typename ForwardIterator>
 std::string
 format_list(ForwardIterator begin, ForwardIterator end)
@@ -606,15 +614,6 @@ to_lower(const std::string &s)
 
 } // namespace util
 
-// pretty printer for std::pair<A, B>
-template <typename A, typename B>
-inline std::ostream &
-operator<<(std::ostream &o, const std::pair<A, B> &p)
-{
-  o << "[" << p.first << ", " << p.second << "]";
-  return o;
-}
-
 // pretty printer for std::vector<T, Alloc>
 template <typename T, typename Alloc>
 static std::ostream &
diff --git a/tailbench/specjbb/build.sh b/tailbench/specjbb/build.sh
index 8f2290e..4891c73 100755
--- a/tailbench/specjbb/build.sh
+++ b/tailbench/specjbb/build.sh
@@ -4,6 +4,25 @@ DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
 TBENCH_PATH=../harness
+TAILBENCH_HOME="$( cd "${DIR}/../.." && pwd )"
+
+ANT_BIN="$(command -v ant || true)"
+LOCAL_ANT=${TAILBENCH_HOME}/.local/ant/bin/ant
+if [[ -z ${ANT_BIN} && -x ${LOCAL_ANT} ]]
+then
+    ANT_BIN=${LOCAL_ANT}
+fi
+if [[ -z ${ANT_BIN} ]]
+then
+    echo "ERROR: ant not found (install ant or ${TAILBENCH_HOME}/.local/ant/bin/ant)."
+    exit 1
+fi
+
+if [[ ! -x ${JDK_PATH}/bin/java ]]
+then
+    echo "ERROR: Java not found at ${JDK_PATH}/bin/java"
+    exit 1
+fi
 
 # Compile client module
 g++ -std=c++0x -g -O3 -fPIC -I${TBENCH_PATH} -c client.cpp -o client.o
@@ -19,4 +38,5 @@ g++ -std=c++11 -g -O3 -shared -fPIC -o libtbench_networked_jni.so \
     ${TBENCH_PATH}/tbench_jni.o ${TBENCH_PATH}/client.o \
     ${TBENCH_PATH}/tbench_server_networked.o -lrt -pthread
 
-env PATH=${JDK_PATH}/bin/:${PATH} CLASSPATH=${TBENCH_PATH}/tbench.jar ant
+env PATH=${JDK_PATH}/bin:$(dirname "${ANT_BIN}"):${PATH} \
+    CLASSPATH=${TBENCH_PATH}/tbench.jar ${ANT_BIN}
diff --git a/tailbench/specjbb/run.sh b/tailbench/specjbb/run.sh
index a68fcf9..adac446 100755
--- a/tailbench/specjbb/run.sh
+++ b/tailbench/specjbb/run.sh
@@ -14,23 +14,36 @@ export LD_LIBRARY_PATH=${TBENCH_PATH}:${LD_LIBRARY_PATH}
 export CLASSPATH=./build/dist/jbb.jar:./build/dist/check.jar:${TBENCH_PATH}/tbench.jar
 
 export PATH=${JDK_PATH}/bin:${PATH}
-export TBENCH_QPS=5000 
-export TBENCH_MAXREQS=25000 
-export TBENCH_WARMUPREQS=25000 
+export TBENCH_QPS=${QPS:-5000}
+export TBENCH_MAXREQS=${MAXREQS:-25000}
+export TBENCH_WARMUPREQS=${WARMUPREQS:-25000}
 export TBENCH_MINSLEEPNS=10000
 
-if [[ -d libtbench_jni.so ]] 
+if [[ ! -x ${JDK_PATH}/bin/java ]]
+then
+    echo "ERROR: Java not found at ${JDK_PATH}/bin/java"
+    exit 1
+fi
+
+RT_PREFIX=()
+if chrt -r 99 /bin/true >/dev/null 2>&1
+then
+    RT_PREFIX=(chrt -r 99)
+else
+    echo "WARN: realtime priority unavailable, running without chrt"
+fi
+
+if [[ -e libtbench_jni.so ]] 
 then
     rm libtbench_jni.so
 fi
 ln -sf ./libtbench_integrated_jni.so libtbench_jni.so
 
-chrt -r 99 ${JDK_PATH}/bin/java -Djava.library.path=. -XX:ParallelGCThreads=32 \
+"${RT_PREFIX[@]}" ${JDK_PATH}/bin/java -Djava.library.path=. -XX:ParallelGCThreads=${GC_THREADS:-32} \
     -XX:+UseSerialGC -XX:NewRatio=1 -XX:NewSize=7000m -Xloggc:gc.log \
     -Xms10000m -Xmx10000m -Xrs spec.jbb.JBBmain -propfile SPECjbb_mt.props
 
 # Teardown
 rm libtbench_jni.so
-rm gc.log
-rm -r results
-
+rm -f gc.log
+rm -rf results
diff --git a/tailbench/specjbb/run_networked.sh b/tailbench/specjbb/run_networked.sh
index 5591f67..5b34d38 100755
--- a/tailbench/specjbb/run_networked.sh
+++ b/tailbench/specjbb/run_networked.sh
@@ -20,13 +20,27 @@ export TBENCH_MAXREQS=25000
 export TBENCH_WARMUPREQS=25000 
 export TBENCH_MINSLEEPNS=10000
 
-if [[ -d libtbench_jni.so ]] 
+if [[ ! -x ${JDK_PATH}/bin/java ]]
+then
+    echo "ERROR: Java not found at ${JDK_PATH}/bin/java"
+    exit 1
+fi
+
+RT_PREFIX=()
+if chrt -r 99 /bin/true >/dev/null 2>&1
+then
+    RT_PREFIX=(chrt -r 99)
+else
+    echo "WARN: realtime priority unavailable, running without chrt"
+fi
+
+if [[ -e libtbench_jni.so ]] 
 then
     rm libtbench_jni.so
 fi
 ln -sf libtbench_networked_jni.so libtbench_jni.so
 
-chrt -r 99 ${JDK_PATH}/bin/java -Djava.library.path=. -XX:ParallelGCThreads=1 \
+"${RT_PREFIX[@]}" ${JDK_PATH}/bin/java -Djava.library.path=. -XX:ParallelGCThreads=1 \
     -XX:+UseSerialGC -XX:NewRatio=1 -XX:NewSize=7000m -Xloggc:gc.log \
     -Xms10000m -Xmx10000m -Xrs spec.jbb.JBBmain -propfile SPECjbb_mt.props &
 
@@ -34,7 +48,7 @@ echo $! > server.pid
 
 sleep 10
 
-chrt -r 99 ./client &
+"${RT_PREFIX[@]}" ./client &
 echo $! > client.pid
 
 wait $(cat client.pid)
@@ -42,6 +56,5 @@ wait $(cat client.pid)
 # Teardown
 ./kill_networked.sh
 rm libtbench_jni.so
-rm gc.log
-rm -r results
-
+rm -f gc.log
+rm -rf results
diff --git a/tailbench/sphinx/run.sh b/tailbench/sphinx/run.sh
index cd0eafe..599e447 100755
--- a/tailbench/sphinx/run.sh
+++ b/tailbench/sphinx/run.sh
@@ -3,10 +3,13 @@
 DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
-THREADS=32
-AUDIO_SAMPLES='audio_samples'
+THREADS=${THREADS:-32}
+AUDIO_SAMPLES=${AUDIO_SAMPLES:-audio_samples}
+QPS=${QPS:-1}
+MAXREQS=${MAXREQS:-25}
+WARMUPREQS=${WARMUPREQS:-10}
 
 LD_LIBRARY_PATH=./sphinx-install/lib:${LD_LIBRARY_PATH} \
-    TBENCH_QPS=1 TBENCH_MAXREQS=25 TBENCH_WARMUPREQS=10 TBENCH_MINSLEEPNS=10000 \
+    TBENCH_QPS=${QPS} TBENCH_MAXREQS=${MAXREQS} TBENCH_WARMUPREQS=${WARMUPREQS} TBENCH_MINSLEEPNS=10000 \
     TBENCH_AN4_CORPUS=${DATA_ROOT}/sphinx TBENCH_AUDIO_SAMPLES=${AUDIO_SAMPLES} \
     ./decoder_integrated -t $THREADS
diff --git a/tailbench/xapian/build.sh b/tailbench/xapian/build.sh
index f67a9cb..0a84033 100755
--- a/tailbench/xapian/build.sh
+++ b/tailbench/xapian/build.sh
@@ -1,16 +1,21 @@
-#/bin/bash
+#!/bin/bash
+
+set -euo pipefail
+
+XAPIAN_CORE_DIR=xapian-core-1.2.13
+XAPIAN_CONFIG=${XAPIAN_CORE_DIR}/install/bin/xapian-config
 
 # Install Xapian core if not already present
-if [[ ! -d xapian-core-1.2.13 ]]
+if [[ ! -x ${XAPIAN_CONFIG} ]]
 then
-    tar -xf xapian-core-1.2.13.tar.gz
-    cd xapian-core-1.2.13
-    mkdir install
+    [[ -d ${XAPIAN_CORE_DIR} ]] || tar -xf ${XAPIAN_CORE_DIR}.tar.gz
+    cd ${XAPIAN_CORE_DIR}
+    mkdir -p install
     ./configure --prefix=$PWD/install CXXFLAGS='-std=c++98'
-    make -j16
+    make -j"$(nproc)"
     make install
     cd -
 fi
 
 # Build search engine
-make
+make -j"$(nproc)"
diff --git a/tailbench/xapian/run.sh b/tailbench/xapian/run.sh
index c9aef1a..a4874f3 100755
--- a/tailbench/xapian/run.sh
+++ b/tailbench/xapian/run.sh
@@ -3,12 +3,13 @@
 DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
-export LD_LIBRARY_PATH=/home/kernel_interference/tailbench/tailbench-v0.9/xapian/xapian-core-1.2.13/install/lib
+XAPIAN_LIB_PATH=${DIR}/xapian-core-1.2.13/install/lib
+export LD_LIBRARY_PATH=${XAPIAN_LIB_PATH}:${LD_LIBRARY_PATH:-}
 
-NSERVERS=32
-QPS=50
-WARMUPREQS=2500
-REQUESTS=5000
+NSERVERS=${NSERVERS:-32}
+QPS=${QPS:-50}
+WARMUPREQS=${WARMUPREQS:-2500}
+REQUESTS=${REQUESTS:-5000}
 
 TBENCH_QPS=${QPS} TBENCH_MAXREQS=${REQUESTS} TBENCH_WARMUPREQS=${WARMUPREQS} \
        TBENCH_MINSLEEPNS=100000 TBENCH_TERMS_FILE=${DATA_ROOT}/xapian/terms.in \
diff --git a/tailbench/xapian/run_networked.sh b/tailbench/xapian/run_networked.sh
index 403e6e3..ee3fdfe 100755
--- a/tailbench/xapian/run_networked.sh
+++ b/tailbench/xapian/run_networked.sh
@@ -3,8 +3,8 @@
 DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
 source ${DIR}/../configs.sh
 
-# 必须要specify一下这个libxapian.so的位置，不然在启动程序时，它在通用的位置找不到它
-export LD_LIBRARY_PATH=/home/kernel_interference/tailbench/tailbench-v0.9/xapian/xapian-core-1.2.13/install/lib
+XAPIAN_LIB_PATH=${DIR}/xapian-core-1.2.13/install/lib
+export LD_LIBRARY_PATH=${XAPIAN_LIB_PATH}:${LD_LIBRARY_PATH:-}
 
 NSERVERS=1
 QPS=50
