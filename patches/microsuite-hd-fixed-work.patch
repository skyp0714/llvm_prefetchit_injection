diff --git a/src/HDSearch/load_generator/load_generator_closed_loop.cc b/src/HDSearch/load_generator/load_generator_closed_loop.cc
index 91a0134..87acda1 100644
--- a/src/HDSearch/load_generator/load_generator_closed_loop.cc
+++ b/src/HDSearch/load_generator/load_generator_closed_loop.cc
@@ -2,11 +2,13 @@
    Ph.D. Candidate at the University of Michigan - Ann Arbor*/
 
 #include <iostream>
+#include <fstream>
 #include <memory>
 #include <random>
 #include <stdlib.h> 
 #include <string>
 #include <sys/time.h>
+#include <unistd.h>
 
 #include <grpc++/grpc++.h>
 #include <thread>
@@ -240,6 +242,14 @@ class LoadGenIndexClient {
             qps_file_name = load_gen_command_line_args->qps_file_name;
             timing_file_name = load_gen_command_line_args->timing_file_name;
             std::string util_file_name = load_gen_command_line_args->util_file_name;
+            uint64_t fixed_requests = 0;
+            if (const char* value = std::getenv("PREFETCHIT_FIXED_REQUESTS")) {
+                fixed_requests = std::strtoull(value, nullptr, 10);
+            }
+            uint64_t fixed_warmup_requests = 0;
+            if (const char* value = std::getenv("PREFETCHIT_FIXED_WARMUP_REQUESTS")) {
+                fixed_warmup_requests = std::strtoull(value, nullptr, 10);
+            }
             CHECK((time_duration >= 0), "ERROR: Offered load (time in seconds) must always be a positive value");
             struct TimingInfo timing_info;
             // Create queries from query file.
@@ -269,7 +279,8 @@ class LoadGenIndexClient {
             double exit_time = warm_up + double((time_duration)*1000000);
             bool flag = false;
 
-            while(curr_time < warm_up) {
+            while((fixed_warmup_requests > 0 && requests_sent < fixed_warmup_requests) ||
+                    (fixed_warmup_requests == 0 && curr_time < warm_up)) {
                 outstanding_mutex.lock();
                 if (outstanding < qps) {
                     outstanding++;
@@ -290,14 +301,48 @@ class LoadGenIndexClient {
                 }
                 curr_time = (double)GetTimeInMicro();
             }
+            while (true) {
+                outstanding_mutex.lock();
+                const bool drained = outstanding == 0;
+                outstanding_mutex.unlock();
+                if (drained) {
+                    break;
+                }
+                usleep(1000);
+            }
             srand(PREFETCHIT_QUERY_SEED);
             query_id = rand() % queries_total;
             query.SetPoint(0, queries.GetPointAtIndex(query_id));
+            requests_sent = 0;
+            util_requests = 0;
+            interval_start_time = GetTimeInSec();
+            responses_recvd_mutex.lock();
+            responses_recvd = 0;
+            responses_failed = 0;
+            responses_recvd_mutex.unlock();
+
+            const char* ready_file = std::getenv("PREFETCHIT_MEASUREMENT_READY_FILE");
+            if (ready_file != nullptr && ready_file[0] != '\0') {
+                std::ofstream ready(ready_file);
+                ready << "ready\n";
+                ready.close();
+            }
+            const char* start_file = std::getenv("PREFETCHIT_MEASUREMENT_START_FILE");
+            if (start_file != nullptr && start_file[0] != '\0') {
+                while (access(start_file, F_OK) != 0) {
+                    usleep(1000);
+                }
+            }
+
+            curr_time = (double)GetTimeInMicro();
+            exit_time = curr_time + double((time_duration)*1000000);
+            const uint64_t measurement_start = GetTimeInMicro();
             start_counter_mutex.lock();
             start_counter = true;
             start_counter_mutex.unlock();
             flag = false;
-            while(curr_time < exit_time) {
+            while((fixed_requests > 0 && requests_sent < fixed_requests) ||
+                    (fixed_requests == 0 && curr_time < exit_time)) {
                 outstanding_mutex.lock();
                 if (outstanding < qps) {
                     outstanding++;
@@ -307,7 +352,12 @@ class LoadGenIndexClient {
                 }
                 outstanding_mutex.unlock();
                 if (flag) {
-                    if((requests_sent == 0) || ((GetTimeInSec() - interval_start_time) >= 10)){
+                    const bool fixed_util_request = fixed_requests > 0 &&
+                        (requests_sent % 40000 == 0);
+                    const bool timed_util_request = fixed_requests == 0 &&
+                        ((requests_sent == 0) ||
+                         ((GetTimeInSec() - interval_start_time) >= 10));
+                    if(fixed_util_request || timed_util_request){
                         util_requests++;
                         loadgen_index.LoadGen_Index(&query,
                                 query_id,
@@ -333,12 +383,54 @@ class LoadGenIndexClient {
                 curr_time = (double)GetTimeInMicro();
             }
 
+            uint64_t fixed_runtime_us = 0;
+            bool fixed_window_overrun = false;
+            if (fixed_requests > 0) {
+                while (true) {
+                    responses_recvd_mutex.lock();
+                    const bool complete =
+                        responses_recvd + responses_failed >= static_cast<int>(fixed_requests);
+                    responses_recvd_mutex.unlock();
+                    if (complete) {
+                        break;
+                    }
+                    usleep(1000);
+                }
+                fixed_runtime_us = GetTimeInMicro() - measurement_start;
+                fixed_window_overrun = fixed_runtime_us > time_duration * 1000000ULL;
+
+                const char* done_file = std::getenv("PREFETCHIT_MEASUREMENT_DONE_FILE");
+                if (done_file != nullptr && done_file[0] != '\0') {
+                    std::ofstream done(done_file);
+                    done << "done\n";
+                    done.close();
+                }
+
+                const char* exit_after_fixed =
+                    std::getenv("PREFETCHIT_EXIT_AFTER_FIXED_WORK");
+                if (exit_after_fixed == nullptr || std::string(exit_after_fixed) == "0") {
+                    while (GetTimeInMicro() < static_cast<uint64_t>(exit_time)) {
+                        usleep(1000);
+                    }
+                }
+            }
+
             responses_recvd_mutex.lock();
             std::cout << std::endl;
             std::cout << responses_recvd << std::endl;
-            std::cout << (static_cast<double>(responses_recvd)/static_cast<double>(time_duration)) << std::endl;
+            if (fixed_requests > 0) {
+                const double fixed_qps = static_cast<double>(fixed_requests) * 1000000.0 /
+                    static_cast<double>(fixed_runtime_us);
+                std::cout << fixed_qps << std::endl;
+                std::cout << "prefetchit_fixed_requests=" << fixed_requests << std::endl;
+                std::cout << "prefetchit_fixed_warmup_requests=" << fixed_warmup_requests << std::endl;
+                std::cout << "prefetchit_fixed_runtime_us=" << fixed_runtime_us << std::endl;
+                std::cout << "prefetchit_fixed_qps=" << fixed_qps << std::endl;
+                std::cout << "prefetchit_fixed_window_overrun=" << fixed_window_overrun << std::endl;
+            } else {
+                std::cout << (static_cast<double>(responses_recvd)/static_cast<double>(time_duration)) << std::endl;
+            }
             std::cout << "responses=" << responses_recvd << std::endl;
-            std::cout << "qps=" << (static_cast<double>(responses_recvd)/static_cast<double>(time_duration)) << std::endl;
             std::cout << "failed_responses=" << responses_failed << std::endl;
             std::cout.flush();
             //PrintTimingHistogram(start_vec);
